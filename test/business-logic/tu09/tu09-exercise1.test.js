import { processAnswer } from "../../../docs/tu09/exercise1.js";
import { showTotal } from "../../../docs/tu09/exercise1.js";

describe("Test Suite Ex1 Arrays", () => {
  it("creates an array with the values given", () => {
    expect(processAnswer(5));
  });
});
